# Arguments by parser

```
python3 arguments.py -h

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Path do input
  -o OUTPUT, --output OUTPUT
                        Path do output
  -a ANOVA_ONE, --anova-one ANOVA_ONE
                        ANOVA one-way
  -b ANOVA_TWO, --anova-two ANOVA_TWO
                        ANOVA two-way
  -c REGRESSION, --regression REGRESSION
                        Regressão linear
  -x FACTOR1, --factor1 FACTOR1
                        Variavel independente
  -w FACTOR2, --factor2 FACTOR2
                        Variavel independente
  -z FACTOR3, --factor3 FACTOR3
                        Variavel independente
  -r RESPONSE, --response RESPONSE
                        Variavel dependente

``` 